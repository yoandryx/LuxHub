---
phase: 06-mainnet-deployment-production-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Solana-Anchor/Anchor.toml
autonomous: false
requirements:
  - MN-01
  - MN-02
  - MN-03
  - MN-06
  - MN-07

user_setup:
  - service: solana-mainnet
    why: "Fund treasury wallet with ~2 SOL for program deployment and on-chain config"
    env_vars: []
    dashboard_config:
      - task: "Send ~2 SOL to treasury wallet on mainnet-beta"
        location: "Phantom wallet or solana CLI transfer"
  - service: squads-protocol
    why: "Create multisig on mainnet via Squads web UI"
    dashboard_config:
      - task: "Create a new multisig at app.squads.so on mainnet with at least 1 member"
        location: "https://app.squads.so"

must_haves:
  truths:
    - "Anchor program is live on mainnet-beta and responds to RPC calls"
    - "EscrowConfig PDA exists on mainnet with fee_bps=300 and correct treasury"
    - "Squads multisig PDA exists on mainnet"
    - "Bags partner config PDA exists on mainnet"
    - "Security scan completed with no critical findings"
  artifacts:
    - path: "Solana-Anchor/Anchor.toml"
      provides: "Mainnet program configuration"
      contains: "[programs.mainnet]"
  key_links:
    - from: "Solana-Anchor/Anchor.toml"
      to: "mainnet program ID"
      via: "anchor deploy output"
      pattern: "programs.mainnet"
---

<objective>
Deploy the LuxHub Anchor program to Solana mainnet-beta and initialize all on-chain configuration (EscrowConfig, Squads multisig, Bags partner config PDA).

Purpose: This is the critical-path deployment that puts the escrow marketplace program on mainnet. All subsequent env var switches and validation depend on this program being live.
Output: Deployed program ID, initialized EscrowConfig PDA, Squads multisig PDA address, Bags partner config PDA address.
</objective>

<execution_context>
@/home/ycstudio/LuxHub/.claude/get-shit-done/workflows/execute-plan.md
@/home/ycstudio/LuxHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mainnet-deployment-production-ops/06-CONTEXT.md
@.planning/phases/06-mainnet-deployment-production-ops/06-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Fund treasury wallet and create Squads multisig on mainnet</name>
  <what-built>Nothing yet -- this is a prerequisite step requiring manual actions that Claude cannot perform.</what-built>
  <how-to-verify>
    1. Treasury wallet needs ~2 SOL on mainnet-beta. Check balance:
       `solana balance <TREASURY_WALLET_ADDRESS> --url mainnet-beta`
       Expected: >= 2 SOL
    2. Create a Squads multisig at https://app.squads.so on mainnet-beta with at least 1 member (your admin wallet).
       Record the multisig PDA address.
    3. Provide:
       - Mainnet deploy wallet keypair path (e.g., ~/LuxHub/Solana-Anchor/keys/mainnet-wallet.json)
       - Squads multisig PDA address
       - Confirm treasury wallet has >= 2 SOL
  </how-to-verify>
  <resume-signal>Provide: (1) mainnet wallet keypair path, (2) Squads multisig PDA address, (3) confirm balance >= 2 SOL</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deploy Anchor program and initialize on-chain config</name>
  <read_first>
    - Solana-Anchor/Anchor.toml
    - Solana-Anchor/programs/luxhub-marketplace/src/lib.rs
    - Solana-Anchor/programs/luxhub-marketplace/src/constants.rs
    - Solana-Anchor/programs/luxhub-marketplace/src/state/config.rs
    - src/idl/luxhub_marketplace.json
  </read_first>
  <files>Solana-Anchor/Anchor.toml</files>
  <action>
    Per D-04, execute in strict order:

    1. **Add mainnet-beta section to Anchor.toml:**
       Add under existing [programs.devnet] section:
       ```toml
       [programs.mainnet]
       luxhub_marketplace = "PLACEHOLDER_UNTIL_DEPLOY"
       ```
       Keep existing devnet config intact.

    2. **Build the Anchor program:**
       ```bash
       cd Solana-Anchor
       anchor build
       ```

    3. **Deploy to mainnet-beta:**
       ```bash
       anchor deploy --provider.cluster mainnet --provider.wallet <KEYPAIR_PATH_FROM_TASK_1>
       ```
       Capture the output program ID (it will be different from devnet ID `kW2w2pHhAP8hFGRLganziunchKu6tjaXyomvF6jxNpj`).

    4. **Update Anchor.toml** with the real mainnet program ID:
       ```toml
       [programs.mainnet]
       luxhub_marketplace = "<NEW_MAINNET_PROGRAM_ID>"
       ```

    5. **Initialize EscrowConfig on mainnet** (per MN-02):
       Use the Anchor CLI or a script to call `initialize_config` with:
       - `authority`: admin wallet pubkey
       - `treasury`: Squads vault PDA (derived from multisig PDA provided in Task 1, vault index 0)
       - `fee_bps`: 300 (3% fee per existing on-chain config)
       - `paused`: false

    6. **Create Bags partner config PDA on mainnet** (per MN-06):
       Call the `/api/bags/create-partner-config` endpoint or use the Bags SDK directly to create the partner config PDA on mainnet.

    7. **Record all outputs** for the SUMMARY:
       - New mainnet program ID
       - EscrowConfig PDA address
       - Squads vault PDA address
       - Bags partner config PDA address

    MN-07 is already satisfied per D-11 (cargo clippy clean, cargo audit done, manual review completed with 3 findings fixed).
    MN-05 (Irys mainnet) is satisfied by setting IRYS_NETWORK=mainnet in Vercel env vars (done in Plan 04).
  </action>
  <verify>
    <automated>cd /home/ycstudio/LuxHub/Solana-Anchor && anchor build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Solana-Anchor/Anchor.toml contains `[programs.mainnet]` section with a valid base58 program ID (not "PLACEHOLDER")
    - `solana program show <PROGRAM_ID> --url mainnet-beta` returns program info (not "Error")
    - EscrowConfig PDA exists on mainnet (verifiable via `solana account <CONFIG_PDA> --url mainnet-beta`)
    - anchor build completes without errors
  </acceptance_criteria>
  <done>
    Anchor program deployed on mainnet-beta with new program ID. EscrowConfig PDA initialized with fee_bps=300, treasury pointing to Squads vault. Bags partner config PDA created. All addresses recorded for env var cutover in Plan 04.
  </done>
</task>

</tasks>

<verification>
- `anchor build` succeeds in Solana-Anchor/
- Anchor.toml has [programs.mainnet] with real program ID
- On-chain: program exists at the deployed address on mainnet-beta
- On-chain: EscrowConfig PDA exists with correct fee_bps and treasury
</verification>

<success_criteria>
- Anchor program is deployed and live on mainnet-beta
- EscrowConfig is initialized with fee_bps=300 and Squads vault as treasury
- Squads multisig exists on mainnet (created by user in Task 1)
- Bags partner config PDA exists on mainnet
- All new addresses are captured for Plan 04 env var cutover
</success_criteria>

<output>
After completion, create `.planning/phases/06-mainnet-deployment-production-ops/06-01-SUMMARY.md`

**Critical output for downstream plans:** Record the following values in the SUMMARY for Plan 04:
- NEW_MAINNET_PROGRAM_ID
- ESCROW_CONFIG_PDA
- SQUADS_MULTISIG_PDA
- SQUADS_VAULT_PDA
- BAGS_PARTNER_CONFIG_PDA
</output>
