---
phase: 06-mainnet-deployment-production-ops
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - .env.example
autonomous: false
requirements:
  - MN-04
  - MN-05
  - OPS-02
  - OPS-03

must_haves:
  truths:
    - "The app loads on luxhub.gold without errors after env var cutover"
    - "All on-chain calls target mainnet-beta (not devnet)"
    - "Sentry captures errors and sends email alerts on new issues"
    - "RPC backup URL is stored in Vercel for manual failover"
  artifacts:
    - path: ".env.example"
      provides: "Complete env var reference for mainnet deployment"
      contains: "R2_ACCOUNT_ID"
  key_links:
    - from: "Vercel env vars"
      to: "src/lib/solana/clusterConfig.ts"
      via: "NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta"
      pattern: "mainnet-beta"
---

<objective>
Switch all Vercel environment variables from devnet to mainnet, configure Sentry alert rules, set RPC backup URL, and verify the app loads correctly on mainnet. This is the final cutover step.

Purpose: After the Anchor program is deployed (Plan 01) and code changes are committed (Plans 02-03), this plan performs the actual environment switch. Per D-01, this is a full cutover with no parallel environments.
Output: App running on mainnet, Sentry alerts configured, .env.example updated.
</objective>

<execution_context>
@/home/ycstudio/LuxHub/.claude/get-shit-done/workflows/execute-plan.md
@/home/ycstudio/LuxHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mainnet-deployment-production-ops/06-CONTEXT.md
@.planning/phases/06-mainnet-deployment-production-ops/06-RESEARCH.md
@.planning/phases/06-mainnet-deployment-production-ops/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .env.example and prepare env var cutover checklist</name>
  <read_first>
    - .env.example
    - src/lib/solana/clusterConfig.ts
    - src/lib/solana/priorityFees.ts
  </read_first>
  <files>.env.example</files>
  <action>
    Update `.env.example` to include all mainnet and new env vars. Add these entries (preserving existing ones):

    ```
    # Solana Network
    NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
    NEXT_PUBLIC_SOLANA_ENDPOINT=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY
    SOLANA_ENDPOINT_BACKUP=https://solana-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
    PROGRAM_ID=NEW_MAINNET_PROGRAM_ID_FROM_PLAN_01

    # Solana Fees
    NEXT_PUBLIC_PRIORITY_FEE_MICRO_LAMPORTS=50000

    # Irys Storage
    IRYS_NETWORK=mainnet

    # Squads Multisig
    NEXT_PUBLIC_SQUADS_MSIG=MAINNET_MULTISIG_PDA_FROM_PLAN_01

    # Cloudflare R2 (replaces IBM COS)
    R2_ACCOUNT_ID=your_cloudflare_account_id
    R2_ACCESS_KEY_ID=your_r2_api_token_access_key
    R2_SECRET_ACCESS_KEY=your_r2_api_token_secret_key
    R2_PUBLIC_DOMAIN=your_r2_public_domain_or_custom_domain

    # Vercel Cron
    CRON_SECRET=generate_random_16_plus_char_string

    # MongoDB (separate mainnet database per D-02)
    MONGODB_URI=mongodb+srv://...mainnet_database...
    ```

    Remove any IBM COS env var entries (IBM_COS_API_KEY, IBM_COS_RESOURCE_INSTANCE_ID) from .env.example.

    Also prepare the complete checklist of Vercel env vars to switch (from RESEARCH.md):

    | Env Var | Mainnet Value |
    |---------|---------------|
    | NEXT_PUBLIC_SOLANA_NETWORK | mainnet-beta |
    | NEXT_PUBLIC_SOLANA_ENDPOINT | https://mainnet.helius-rpc.com/?api-key=<KEY> |
    | SOLANA_ENDPOINT_BACKUP | https://solana-mainnet.g.alchemy.com/v2/<KEY> (NEW) |
    | PROGRAM_ID | <from Plan 01 deploy output> |
    | MONGODB_URI | <new mainnet database URI per D-02> |
    | IRYS_NETWORK | mainnet |
    | NEXT_PUBLIC_PRIORITY_FEE_MICRO_LAMPORTS | 50000 |
    | NEXT_PUBLIC_SQUADS_MSIG | <from Plan 01 / Task 1> |
    | CRON_SECRET | <random 16+ char string> |
    | R2_ACCOUNT_ID | <from Cloudflare dashboard> |
    | R2_ACCESS_KEY_ID | <from R2 API token> |
    | R2_SECRET_ACCESS_KEY | <from R2 API token> |
    | R2_PUBLIC_DOMAIN | <from R2 bucket public access settings> |

    Env vars that stay the same (no change):
    JWT_SECRET, PII_ENCRYPTION_KEY, PII_HASH_SALT, ADMIN_WALLETS,
    NEXT_PUBLIC_PINATA_API_KEY, NEXT_PUBLIC_PINATA_SECRET_KEY,
    ANTHROPIC_API_KEY, BAGS_API_KEY, BAGS_WEBHOOK_SECRET,
    NEXT_PUBLIC_LUXHUB_WALLET, NEXT_PUBLIC_APP_URL
  </action>
  <verify>
    <automated>cd /home/ycstudio/LuxHub && grep -c "R2_ACCOUNT_ID\|CRON_SECRET\|SOLANA_ENDPOINT_BACKUP\|IRYS_NETWORK\|PRIORITY_FEE" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_PUBLIC_DOMAIN
    - .env.example contains CRON_SECRET
    - .env.example contains SOLANA_ENDPOINT_BACKUP
    - .env.example contains IRYS_NETWORK=mainnet
    - .env.example contains NEXT_PUBLIC_PRIORITY_FEE_MICRO_LAMPORTS=50000
    - .env.example contains NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
    - .env.example does NOT contain IBM_COS_API_KEY or IBM_COS_RESOURCE_INSTANCE_ID
  </acceptance_criteria>
  <done>.env.example updated with all mainnet env vars. IBM COS entries removed. Complete cutover checklist documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Switch Vercel env vars to mainnet and verify app loads</name>
  <what-built>All code changes committed (Plans 01-03). .env.example updated with mainnet values. The platform is ready for the environment variable switch per D-01 (full cutover).</what-built>
  <how-to-verify>
    **Step 1: Switch env vars in Vercel dashboard (https://vercel.com/dashboard):**

    Update these env vars in Vercel project settings -> Environment Variables:
    1. NEXT_PUBLIC_SOLANA_NETWORK = `mainnet-beta`
    2. NEXT_PUBLIC_SOLANA_ENDPOINT = `https://mainnet.helius-rpc.com/?api-key=<YOUR_KEY>`
    3. PROGRAM_ID = `<mainnet program ID from Plan 01>`
    4. MONGODB_URI = `<new mainnet MongoDB URI per D-02>`
    5. IRYS_NETWORK = `mainnet`
    6. NEXT_PUBLIC_SQUADS_MSIG = `<mainnet multisig PDA from Plan 01>`
    7. NEXT_PUBLIC_PRIORITY_FEE_MICRO_LAMPORTS = `50000`

    Add NEW env vars:
    8. SOLANA_ENDPOINT_BACKUP = `https://solana-mainnet.g.alchemy.com/v2/<YOUR_KEY>` (per D-06/D-07)
    9. CRON_SECRET = `<random 16+ char string>`
    10. R2_ACCOUNT_ID = `<from Cloudflare dashboard>`
    11. R2_ACCESS_KEY_ID = `<from R2 API token>`
    12. R2_SECRET_ACCESS_KEY = `<from R2 API token>`
    13. R2_PUBLIC_DOMAIN = `<from R2 public access settings>`

    **Step 2: Trigger Vercel redeploy** (push a commit or use Vercel dashboard -> Deployments -> Redeploy)

    **Step 3: Verify the app loads:**
    1. Visit https://luxhub.gold — page should load without errors
    2. Open browser console — no "NEXT_PUBLIC_SOLANA_ENDPOINT is not configured" errors
    3. Connect wallet — should connect to mainnet-beta (check Phantom shows "Mainnet Beta")
    4. Visit https://luxhub.gold/marketplace — should load (may show empty if new DB)

    **Step 4: Configure Sentry alert rules** (per OPS-02):
    1. Go to Sentry dashboard -> Alerts -> Create Alert Rule
    2. Create "New Issue" alert: When a new issue is created -> Send email to admin@luxhub.gold
    3. Create "High Frequency" alert: When an issue occurs >10 times in 1 hour -> Send email with "URGENT" prefix to admin@luxhub.gold

    **Step 5: Verify cron is registered:**
    1. In Vercel dashboard -> Project -> Settings -> Cron Jobs
    2. Should show `/api/escrow/enforce-timeouts` with schedule `0 */6 * * *`
    3. If on Hobby plan, it may show as daily — this is acceptable per research

    **Step 6: Verify Dependabot is active:**
    1. Go to GitHub repo -> Settings -> Code security and analysis
    2. Dependabot alerts and Dependabot security updates should be enabled
  </how-to-verify>
  <resume-signal>Type "verified" once the app loads on mainnet, Sentry alerts are configured, and cron is registered. Or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- App loads at https://luxhub.gold without console errors
- Wallet connects to mainnet-beta
- Sentry alert rules exist (new issue + high frequency)
- Vercel cron shows enforce-timeouts scheduled
- .env.example contains all mainnet env vars
</verification>

<success_criteria>
- All Vercel env vars switched to mainnet per D-01
- Separate MongoDB for mainnet per D-02
- App loads and connects to mainnet-beta
- Sentry configured with email alerts on new issues and high-frequency errors (OPS-02)
- RPC backup URL stored in SOLANA_ENDPOINT_BACKUP (OPS-03)
- Vercel cron registered for enforce-timeouts
- Dependabot enabled on GitHub repo
</success_criteria>

<output>
After completion, create `.planning/phases/06-mainnet-deployment-production-ops/06-04-SUMMARY.md`
</output>
