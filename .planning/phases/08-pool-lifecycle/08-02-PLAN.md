---
phase: 08-pool-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/pools/[id].tsx
  - src/components/pool/LifecycleStepper.tsx
  - src/components/pool/TradeWidget.tsx
  - src/components/pool/PositionSummary.tsx
  - src/components/pool/HowItWorks.tsx
  - src/components/pool/ClaimDistribution.tsx
  - src/styles/PoolDetailV2.module.css
  - src/styles/LifecycleStepper.module.css
  - src/styles/TradeWidget.module.css
autonomous: true
requirements: [POOL-06, UI-03, POOL-02, POOL-04]
must_haves:
  truths:
    - "User can view a dedicated pool page at /pools/[id] with chart, trade widget, lifecycle stepper, and position summary"
    - "Lifecycle stepper shows 6 stages with current stage highlighted in purple accent"
    - "User can buy/sell pool tokens from the embedded trade widget on the pool page"
    - "Connected wallet sees their position (token balance, ownership %, P&L) when holding tokens"
    - "After graduation, pool page shows 'Trading on DEX' badge and trading continues"
    - "After resale/distribution, trade widget is replaced with claim panel"
  artifacts:
    - path: "src/pages/pools/[id].tsx"
      provides: "Dedicated pool detail page"
      min_lines: 200
    - path: "src/components/pool/LifecycleStepper.tsx"
      provides: "Horizontal lifecycle timeline component"
      exports: ["LifecycleStepper"]
    - path: "src/components/pool/TradeWidget.tsx"
      provides: "Embedded buy/sell widget"
      exports: ["TradeWidget"]
    - path: "src/components/pool/PositionSummary.tsx"
      provides: "YOUR POSITION card"
      exports: ["PositionSummary"]
    - path: "src/components/pool/ClaimDistribution.tsx"
      provides: "Claim proceeds panel"
      exports: ["ClaimDistribution"]
  key_links:
    - from: "src/pages/pools/[id].tsx"
      to: "/api/pool/[id]"
      via: "SWR fetch for pool data"
      pattern: "useSWR.*api/pool"
    - from: "src/components/pool/TradeWidget.tsx"
      to: "/api/bags/trade-quote"
      via: "fetch for price quote"
      pattern: "trade-quote"
    - from: "src/components/pool/ClaimDistribution.tsx"
      to: "/api/pool/claim-distribution"
      via: "fetch for claim execution"
      pattern: "claim-distribution"
    - from: "src/components/marketplace/TvChart.tsx"
      to: "/api/bags/price-history"
      via: "tokenMint prop passed to TvChart, fetches DexScreener data"
      pattern: "price-history"
---

<objective>
Build the dedicated pool detail page with all sub-components: lifecycle stepper, embedded trade widget, price chart, position summary, claim distribution panel, and how-it-works explainer.

Purpose: This is the primary user-facing deliverable of Phase 8. Per D-05, the pool page is a full dedicated page (not a modal) with shareable URL. Per D-06, it has an embedded trade widget. Per D-12, it has a horizontal lifecycle stepper. Per D-14, it shows the user's position.

Output: New `/pools/[id]` page with 5 sub-components and associated CSS modules.
</objective>

<execution_context>
@/home/ycstudio/LuxHub/.claude/get-shit-done/workflows/execute-plan.md
@/home/ycstudio/LuxHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pool-lifecycle/08-CONTEXT.md
@.planning/phases/08-pool-lifecycle/08-RESEARCH.md
@.planning/phases/08-pool-lifecycle/08-UI-SPEC.md

<interfaces>
<!-- Existing pool page and components -- executor must read these for patterns -->

From src/pages/pool/[id].tsx (existing pool detail page, 830 lines):
- Uses useEffectiveWallet() for wallet connection
- Fetches pool data from /api/pool/[id]
- Uses styles from PoolDetailNew.module.css
- Imports BagsPoolTrading component for trading
- Has Asset and Participant interfaces defined inline

From src/components/marketplace/BagsPoolTrading.tsx (existing trading component, 533 lines):
- Handles buy/sell flow via /api/bags/trade-quote and /api/bags/execute-trade
- Uses useEffectiveWallet() for signing
- Has slippage selector, SOL/token amount input
- Supports both bonding curve and DEX modes

From src/styles/PoolDetailNew.module.css:
- Chrome glass theme with --accent: #c8a1ff
- Glass-morphism card patterns
- Responsive grid layout

From src/styles/LuxHubTheme.css:
- Full design system reference (glass cards, buttons, badges, animations)

From src/components/marketplace/TvChart.tsx:
- Existing price chart component using DexScreener data
- Accepts tokenMint prop
- Pre-graduation: bonding curve data may be limited; use Bags trade webhook data from pool.recentTrades if DexScreener has no data
- Post-graduation: DexScreener data available via /api/bags/price-history

From src/hooks/useEffectiveWallet.ts:
- Returns { publicKey, signTransaction, connected, ... }

<!-- Claim distribution API contract (created in parallel by Plan 08-01) -->
From src/pages/api/pool/claim-distribution.ts:
```typescript
// POST /api/pool/claim-distribution
// Request body: { poolId: string, claimerWallet: string, execute?: boolean }
// Response (execute=false): { claimableAmount: number, ownershipPercent: number, wallet: string, poolId: string }
// Response (execute=true): { success: true, claimableAmount: number, status: 'pending' }
//
// PATCH /api/pool/claim-distribution
// Request body: { poolId: string, claimerWallet: string, txSignature: string }
// Response: { success: true, claimed: true }
//
// POST with action: 'sweep-unclaimed' (admin only)
// Request body: { poolId: string, adminWallet: string, action: 'sweep-unclaimed' }
// Response: { success: true, sweptAmount: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lifecycle stepper, position summary, how-it-works, and claim distribution components</name>
  <files>
    src/components/pool/LifecycleStepper.tsx,
    src/components/pool/PositionSummary.tsx,
    src/components/pool/HowItWorks.tsx,
    src/components/pool/ClaimDistribution.tsx,
    src/styles/LifecycleStepper.module.css
  </files>
  <read_first>
    - src/pages/pool/[id].tsx (existing pool detail page -- understand data shape, interfaces, patterns)
    - src/styles/PoolDetailNew.module.css (existing chrome glass patterns to replicate)
    - src/styles/LuxHubTheme.css (design system reference -- glass cards, badges, animations)
    - .planning/phases/08-pool-lifecycle/08-UI-SPEC.md (full visual spec for all components)
    - .planning/phases/08-pool-lifecycle/08-CONTEXT.md (D-12, D-13, D-14, D-10 decisions)
    - .claude/projects/-home-ycstudio-LuxHub/memory/legal_language_rules.md (legal terms compliance)
    - .claude/projects/-home-ycstudio-LuxHub/memory/fee_model_correct.md (fee model)
  </read_first>
  <action>
    Create `src/components/pool/` directory.

    **1. LifecycleStepper.tsx** (D-12, UI-03):

    Props: `{ currentStage: string, graduated?: boolean }`

    Define `LIFECYCLE_STAGES` array with 6 stages per UI-SPEC:
    ```typescript
    const LIFECYCLE_STAGES = [
      { key: 'launch', label: 'Launch', icon: FaRocket },
      { key: 'funding', label: 'Funding', icon: FaChartLine },
      { key: 'graduated', label: 'Graduated', icon: FaCheckCircle },
      { key: 'trading', label: 'Trading', icon: FaExchangeAlt },
      { key: 'resale', label: 'Resale', icon: FaMoneyBillWave },
      { key: 'distributed', label: 'Distributed', icon: FaUsers },
    ] as const;
    ```

    Export `getLifecycleStage(pool)` function that maps pool status to stage:
    - `['closed', 'distributed']` -> 'distributed'
    - `['sold', 'distributing']` -> 'resale'
    - `['listed', 'active', 'custody']` -> 'trading'
    - `pool.graduated === true` -> 'graduated'
    - `['open', 'filled', 'funded']` -> 'funding'
    - default -> 'launch'

    Render horizontal stepper with:
    - Completed stages: filled purple circle (`#c8a1ff`), solid purple connecting line
    - Current stage: filled purple circle with outer glow ring (`0 0 12px rgba(200, 161, 255, 0.4)`), white label
    - Future stages: hollow circle with `rgba(200, 161, 255, 0.2)` border, dashed line, muted label
    - Circle diameter: 12px, connecting line height: 2px, label 13px below
    - Mobile: `overflow-x: auto` with hidden scrollbar, current stage centered

    **2. PositionSummary.tsx** (D-14):

    Props: `{ tokenBalance: number, ownershipPercent: number, costBasis: number, currentValue: number, poolStatus: string }`

    Show only when `tokenBalance > 0`. Glass card with accent-dim left border (3px solid rgba(200, 161, 255, 0.25)).

    Display fields per UI-SPEC:
    - Token Balance: monospace, comma-separated (e.g., "1,250,000")
    - Ownership: percentage with 2 decimals (e.g., "0.13%")
    - Cost Basis: USD with $ prefix
    - Current Value: USD with $ prefix
    - P&L: signed USD + percentage. Green (`#4ade80`) if positive, red (`#f87171`) if negative

    When `tokenBalance === 0` and poolStatus is 'open' or 'funded', show CTA: "Join this pool" with arrow icon pointing to trade widget area.

    **3. HowItWorks.tsx** (D-13):

    Collapsible section. Collapsed by default (use `useState(false)`).

    Toggle button: "How This Works" with FaChevronDown icon, 14px, muted color. Rotate chevron 180deg when expanded.

    4 steps when expanded:
    1. "Token Launch" -- "A luxury asset is tokenized. Tokens represent participation in the pool."
    2. "Funding the Watch" -- "Contributors buy tokens on a bonding curve. As more people join, the price rises."
    3. "Secondary Trading" -- "After graduation, tokens trade freely on the open market."
    4. "Resale and Distribution" -- "When the asset is sold, 97% of proceeds are distributed proportionally to all token holders."

    Each step: number circle (24px, accent-dim background) + title (14px, weight 600) + description (13px, muted). Collapse animation: 200ms ease, max-height transition.

    LEGAL COMPLIANCE: No "invest", "shares", "ROI", "profit", "fractional ownership", "dividends". Use "participate", "tokens", "contribute", "proceeds", "distributions".

    **4. ClaimDistribution.tsx** (D-10):

    Props: `{ poolId: string, claimerWallet: string, claimableAmount: number, ownershipPercent: number, claimed: boolean, claimWindowExpiresAt?: string, txSignature?: string }`

    States:
    - `claimed === false && claimableAmount > 0`: Show "Claim Your Proceeds" header, amount in display size monospace accent color, "Claim Proceeds" button (full width, accent gradient)
    - `claimed === true`: Green checkmark icon + "Proceeds Claimed" text + amount + txSignature link to Solana explorer
    - Claim window warning: if `claimWindowExpiresAt` and < 30 days remain, show "Claim before [date]" in warning color (`#fbbf24`)
    - Wallet not connected: "Connect wallet to check your distribution"
    - Loading state: spinner during claim execution

    On claim button click: POST to `/api/pool/claim-distribution` with `{ poolId, claimerWallet, execute: true }`. After wallet signs the SOL transfer TX, call PATCH `/api/pool/claim-distribution` with `{ poolId, claimerWallet, txSignature }` to confirm. Show toast on success/failure. (See claim-distribution API contract in <interfaces> above for full request/response shapes.)

    **5. LifecycleStepper.module.css**:

    Use CSS Modules with chrome glass variables at root:
    ```css
    .container { --accent: #c8a1ff; --accent-dim: rgba(200, 161, 255, 0.12); --text-muted: #a1a1a1; }
    ```
    Style stepper stages, connecting lines, glow effect, mobile horizontal scroll.
  </action>
  <verify>
    <automated>test -f src/components/pool/LifecycleStepper.tsx && test -f src/components/pool/PositionSummary.tsx && test -f src/components/pool/HowItWorks.tsx && test -f src/components/pool/ClaimDistribution.tsx && echo "ALL_EXIST"; grep -c "getLifecycleStage" src/components/pool/LifecycleStepper.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 component files exist in `src/components/pool/`
    - `grep "getLifecycleStage" src/components/pool/LifecycleStepper.tsx` returns at least 2 matches (definition + export)
    - `grep "LIFECYCLE_STAGES" src/components/pool/LifecycleStepper.tsx` returns at least 1 match
    - `grep "c8a1ff" src/styles/LifecycleStepper.module.css` returns at least 1 match
    - `grep "claim-distribution" src/components/pool/ClaimDistribution.tsx` returns at least 1 match (API call)
    - `grep "Claim Your Proceeds" src/components/pool/ClaimDistribution.tsx` returns at least 1 match
    - `grep "How This Works" src/components/pool/HowItWorks.tsx` returns at least 1 match
    - `grep "tokenBalance" src/components/pool/PositionSummary.tsx` returns at least 1 match
    - `grep -i "invest\|shares\|ROI\|profit\|fractional ownership\|dividends" src/components/pool/HowItWorks.tsx` returns 0 matches (legal compliance)
  </acceptance_criteria>
  <done>All 4 sub-components render correctly with chrome glass styling. LifecycleStepper maps pool status to 6 lifecycle stages. PositionSummary shows P&L. HowItWorks is legally compliant. ClaimDistribution handles claim flow using the API contract from Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: Dedicated pool detail page with chart integration and trade widget</name>
  <files>
    src/pages/pools/[id].tsx,
    src/components/pool/TradeWidget.tsx,
    src/styles/PoolDetailV2.module.css,
    src/styles/TradeWidget.module.css,
    src/pages/pools.tsx
  </files>
  <read_first>
    - src/pages/pool/[id].tsx (existing pool detail page -- adapt data fetching, interfaces, wallet integration)
    - src/components/marketplace/BagsPoolTrading.tsx (existing trade component -- extract buy/sell logic for TradeWidget)
    - src/components/marketplace/TvChart.tsx (existing chart component -- reuse for price display)
    - src/pages/pools.tsx (existing browse page -- update to link to /pools/[id] instead of modal)
    - src/styles/PoolDetailNew.module.css (existing styles -- reference for chrome glass patterns)
    - src/hooks/useEffectiveWallet.ts (wallet hook interface)
    - .planning/phases/08-pool-lifecycle/08-UI-SPEC.md (full layout spec -- desktop 2-col, mobile single-col)
    - src/components/pool/LifecycleStepper.tsx (just created in Task 1)
    - src/components/pool/PositionSummary.tsx (just created in Task 1)
    - src/components/pool/HowItWorks.tsx (just created in Task 1)
    - src/components/pool/ClaimDistribution.tsx (just created in Task 1)
  </read_first>
  <action>
    **NOTE: This task creates TradeWidget + the pool page + 2 CSS modules + pools.tsx update. It is high complexity (~5 files) but the files are tightly coupled and cannot be meaningfully tested independently. Accept the scope risk and prioritize getting the page rendering end-to-end.**

    **1. Create `src/components/pool/TradeWidget.tsx`** (D-06):

    Extract and refactor the buy/sell logic from `BagsPoolTrading.tsx` into a cleaner component.

    Props: `{ pool: PoolData, onTradeComplete?: () => void }`

    Where `PoolData` includes: `_id, bagsTokenMint, graduated, status, bondingCurveActive, bagsTokenStatus, lastPriceUSD, currentBondingPrice, targetAmountUSD, sharesSold, totalShares`

    Features:
    - Tab bar: Buy | Sell (pill-style tabs, accent background on active)
    - SOL amount input with "Max" button (reads wallet SOL balance). Input height 48px, border-radius 8px, glass background
    - Estimated output: show token amount from `/api/bags/trade-quote` response. Debounce quote fetch 500ms after input change.
    - Execute button: full-width, 48px, accent gradient (`linear-gradient(135deg, #c8a1ff 0%, #a855f7 100%)`), white text, weight 600. Label: "Buy Tokens" or "Sell Tokens"
    - Slippage selector row: 0.5% | 1% | 3% | Custom (small pills)
    - On execute: call `/api/bags/execute-trade` with `{ tokenMint: pool.bagsTokenMint, type: 'buy'|'sell', amountLamports, slippageBps, userWallet }`. Sign returned transaction with `useEffectiveWallet().signTransaction`. Send via connection.sendRawTransaction.
    - After graduation (D-08): show "Trading on DEX" badge in `#4ade80` color above tabs
    - When pool is in `resale`, `distributing`, `distributed`, or `closed` status: hide TradeWidget entirely (parent page handles showing ClaimDistribution instead)
    - Wallet not connected: inputs visible but disabled, button label "Connect Wallet"
    - Loading state during trade: button shows spinner, disabled

    **Bonding curve progress bar** (inside TradeWidget or as separate section):
    - Full-width bar, height 8px, border-radius 4px
    - Background: `rgba(200, 161, 255, 0.1)`
    - Fill: `linear-gradient(90deg, #c8a1ff, #a855f7)`, width = `(pool.sharesSold / pool.totalShares * 100)%` (or calculate from reserve/target)
    - Label above: "{X}% Funded" in 13px weight 600
    - At 100%: fill turns `#4ade80`, label "Fully Funded"

    **2. Create `src/pages/pools/[id].tsx`** (D-05):

    Create `src/pages/pools/` directory.

    New dedicated pool detail page. Adapt data fetching from existing `src/pages/pool/[id].tsx`:
    - Use `useRouter().query.id` to get pool ID
    - Fetch pool data from `/api/pool/${id}` with SWR (pattern from existing page)
    - Fetch user position: check if wallet is connected, then get token balance via RPC (`getTokenAccountsByOwner`) for `pool.bagsTokenMint`
    - Fetch SOL price from `/api/price/sol` for USD conversions

    Page layout per UI-SPEC desktop:
    ```
    [Back link: "< Back to Pools" -> /pools]
    [LifecycleStepper -- full width, max 1280px centered]
    [HowItWorks -- collapsible, full width]
    [Content Grid -- 2 columns, 1280px max, 24px gap]
      [Left Column]                    [Right Column]
      - Watch Image (from asset)       - TradeWidget (or ClaimDistribution)
      - Price Chart (TvChart, 360px)   - PositionSummary (if holding)
      - Pool Details Card              - Pool Info (holders, volume, etc.)
      - Asset Info Card
    ```

    **TvChart data source handling:**
    - Pass `tokenMint={pool.bagsTokenMint}` prop to TvChart
    - Pre-graduation: TvChart may have limited DexScreener data. If no chart data available, show a simplified price display using pool.recentTrades or pool.currentBondingPrice instead of an empty chart.
    - Post-graduation: DexScreener has full OHLCV data via `/api/bags/price-history`. TvChart renders normally.

    Mobile layout (< 768px): single column stack per UI-SPEC order.

    Conditional rendering:
    - Pool status in `['distributed', 'closed']` or `distributionStatus` in `['proposed', 'approved', 'executed']`: show ClaimDistribution instead of TradeWidget
    - Pool status `resale`: show "Resale in Progress" message instead of TradeWidget
    - Default: show TradeWidget

    Import and use `getLifecycleStage()` from LifecycleStepper to determine current stage.

    Head meta tags for shareable URL: `<title>{pool.asset.brand} {pool.asset.model} Pool | LuxHub</title>`

    **3. Create `src/styles/PoolDetailV2.module.css`**:

    Two-column grid layout:
    ```css
    .pageContainer { max-width: 1280px; margin: 0 auto; padding: 80px 32px 48px; }
    .contentGrid { display: grid; grid-template-columns: 1fr 380px; gap: 24px; }
    @media (max-width: 768px) { .contentGrid { grid-template-columns: 1fr; } }
    ```

    Glass card base: `background: rgba(18, 18, 28, 0.65); border: 1px solid #222222; border-radius: 12px; backdrop-filter: blur(20px);`

    Use `--accent: #c8a1ff` throughout. Page background: `#0a0a0f`.

    **4. Create `src/styles/TradeWidget.module.css`**:

    Glass panel styling, tab pills, input styling, gradient button, slippage pills, progress bar.

    **5. Update `src/pages/pools.tsx`** (browse page):

    Change pool card click handler to navigate to `/pools/${pool._id}` instead of opening PoolDetail modal. Remove modal import if no longer needed. Add lifecycle badges to pool cards per UI-SPEC (New, Funding, Graduated, Trading, Resale, Closed) using `getLifecycleStage()`.
  </action>
  <verify>
    <automated>test -f src/pages/pools/\[id\].tsx && test -f src/components/pool/TradeWidget.tsx && echo "EXISTS"; grep -c "LifecycleStepper" src/pages/pools/\[id\].tsx; grep -c "TradeWidget" src/pages/pools/\[id\].tsx; grep -c "ClaimDistribution" src/pages/pools/\[id\].tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/pools/[id].tsx` exists with at least 200 lines
    - File `src/components/pool/TradeWidget.tsx` exists
    - File `src/styles/PoolDetailV2.module.css` exists
    - File `src/styles/TradeWidget.module.css` exists
    - `grep "LifecycleStepper" src/pages/pools/[id].tsx` returns at least 1 match
    - `grep "TradeWidget" src/pages/pools/[id].tsx` returns at least 1 match
    - `grep "ClaimDistribution" src/pages/pools/[id].tsx` returns at least 1 match
    - `grep "PositionSummary" src/pages/pools/[id].tsx` returns at least 1 match
    - `grep "HowItWorks" src/pages/pools/[id].tsx` returns at least 1 match
    - `grep "TvChart" src/pages/pools/[id].tsx` returns at least 1 match
    - `grep "trade-quote" src/components/pool/TradeWidget.tsx` returns at least 1 match
    - `grep "execute-trade" src/components/pool/TradeWidget.tsx` returns at least 1 match
    - `grep "Trading on DEX" src/components/pool/TradeWidget.tsx` returns at least 1 match
    - `grep "1280px" src/styles/PoolDetailV2.module.css` returns at least 1 match
    - `grep "pools/" src/pages/pools.tsx` returns at least 1 match (navigation to new page)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Dedicated /pools/[id] page renders with lifecycle stepper, chart, trade widget (buy/sell), position summary, claim panel, and how-it-works explainer. Browse page links to dedicated page. Desktop shows 2-column layout, mobile single column. Graduated pools show DEX badge. Distribution pools show claim panel. TvChart receives tokenMint prop for price-history data.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with all new pages and components
- `/pools/[id]` page imports all 5 sub-components
- TradeWidget calls /api/bags/trade-quote and /api/bags/execute-trade
- LifecycleStepper renders 6 stages with correct status mapping
- ClaimDistribution calls /api/pool/claim-distribution (contract documented in interfaces)
- TvChart receives tokenMint prop and renders price chart from /api/bags/price-history
- Pools browse page navigates to /pools/[id] instead of modal
</verification>

<success_criteria>
- Pool detail page is a full dedicated page at /pools/[id] with shareable URL (D-05)
- Embedded trade widget with buy/sell tabs, bonding curve progress (D-06)
- Horizontal lifecycle stepper with 6 stages, purple accent on current (D-12, UI-03, POOL-06)
- YOUR POSITION section shows token balance, ownership %, P&L (D-14)
- How This Works collapsible explainer (D-13)
- Claim distribution panel replaces trade widget after resale (D-10)
- Graduated pools show "Trading on DEX" badge (D-08)
- TvChart shows bonding curve data pre-graduation and DexScreener data post-graduation (D-07)
- All user-facing text uses legal-safe language
</success_criteria>

<output>
After completion, create `.planning/phases/08-pool-lifecycle/08-02-SUMMARY.md`
</output>
