---
phase: 08-pool-lifecycle
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - src/pages/pools.tsx
  - src/pages/pool/[id].tsx
autonomous: false
requirements: [POOL-01, POOL-02, POOL-03, POOL-04, POOL-05, POOL-06, UI-03, INFRA-04]
must_haves:
  truths:
    - "Build compiles successfully with all pool lifecycle components"
    - "Pool browse page links to dedicated /pools/[id] page"
    - "Lifecycle stepper renders at all 6 stages"
    - "Trade widget handles buy and sell flows"
    - "Claim distribution panel appears for distributing pools"
    - "Pool creation stepper handles the full Bags launch flow"
    - "Admin can create a pool on behalf of a vendor (D-02)"
    - "Resale price setting creates escrow, buyer purchases, vendor ships, buyer confirms, proceeds distribute (INFRA-04)"
    - "No legal language violations in any pool UI text"
  artifacts:
    - path: "src/pages/pools/[id].tsx"
      provides: "Dedicated pool detail page"
      min_lines: 200
    - path: "src/components/pool/LifecycleStepper.tsx"
      provides: "Lifecycle stepper component"
      contains: "getLifecycleStage"
  key_links:
    - from: "src/pages/pools.tsx"
      to: "/pools/[id]"
      via: "Router navigation on pool card click"
      pattern: "pools/"
---

<objective>
Build validation, redirect wiring, and end-to-end verification of the complete pool lifecycle.

Purpose: Ensures all pool lifecycle components work together. Adds redirect from old /pool/[id] to new /pools/[id]. Validates build succeeds. Human checkpoint verifies visual and functional correctness including the full INFRA-04 resale chain.

Output: Clean build, redirects wired, human verification of pool lifecycle UX.
</objective>

<execution_context>
@/home/ycstudio/LuxHub/.claude/get-shit-done/workflows/execute-plan.md
@/home/ycstudio/LuxHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pool-lifecycle/08-CONTEXT.md
@.planning/phases/08-pool-lifecycle/08-01-SUMMARY.md
@.planning/phases/08-pool-lifecycle/08-02-SUMMARY.md
@.planning/phases/08-pool-lifecycle/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect old pool URL and build validation</name>
  <files>src/pages/pool/[id].tsx, src/pages/pools.tsx</files>
  <read_first>
    - src/pages/pool/[id].tsx (existing pool detail page -- convert to redirect)
    - src/pages/pools/[id].tsx (new pool detail page -- verify it exists and works)
    - src/pages/pools.tsx (browse page -- verify links to /pools/[id])
    - src/components/pool/LifecycleStepper.tsx (verify getLifecycleStage export)
    - src/components/pool/TradeWidget.tsx (verify trade-quote integration)
    - src/components/pool/ClaimDistribution.tsx (verify claim-distribution integration)
    - src/components/pool/PoolCreationStepper.tsx (verify create-pool-token integration and adminMode prop)
    - src/components/pool/PoolManagement.tsx (verify lifecycle stage actions and resale-escrow wiring)
  </read_first>
  <action>
    **1. Convert `src/pages/pool/[id].tsx` to redirect**:

    Replace the existing 830-line pool detail page with a simple redirect to the new `/pools/[id]` page:

    ```typescript
    import { useRouter } from 'next/router';
    import { useEffect } from 'react';

    export default function PoolRedirect() {
      const router = useRouter();
      const { id } = router.query;

      useEffect(() => {
        if (id) {
          router.replace(`/pools/${id}`);
        }
      }, [id, router]);

      return null;
    }
    ```

    This preserves any existing bookmarks/links to `/pool/[id]` while directing users to the new page.

    **2. Verify pools browse page**:

    Ensure `src/pages/pools.tsx` has been updated (from Plan 02) to:
    - Navigate to `/pools/${pool._id}` on pool card click (not modal)
    - Show lifecycle badges on pool cards
    - If not updated, make the changes: replace modal open with `router.push('/pools/${pool._id}')` and add badge from `getLifecycleStage()`

    **3. Run full build validation**:

    Execute `npm run build` to verify all TypeScript compiles, all imports resolve, and no circular dependencies exist.

    **4. Legal language compliance check**:

    Run grep across all pool component files for prohibited terms:
    ```bash
    grep -ri "invest\b\|shares\b\|fractional ownership\|dividend\|\bROI\b\|\bprofit\b" src/components/pool/ src/pages/pools/ src/pages/vendor/pools.tsx
    ```
    Fix any violations found. Replace with legal-safe alternatives per `legal_language_rules.md`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; grep "replace.*pools/" src/pages/pool/\[id\].tsx; grep -ri "invest\b\|shares\b\|fractional ownership\|dividend\|\bROI\b\|\bprofit\b" src/components/pool/ src/pages/pools/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0 (no compilation errors)
    - `grep "replace.*pools/" src/pages/pool/[id].tsx` returns at least 1 match (redirect)
    - `wc -l` of legal language grep returns 0 (no violations)
    - `src/pages/pool/[id].tsx` is under 30 lines (redirect only, not the old 830-line page)
  </acceptance_criteria>
  <done>Old pool URL redirects to new page. Build compiles cleanly. No legal language violations in pool UI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of pool lifecycle</name>
  <files>src/pages/pools/[id].tsx</files>
  <action>
    Human verifies the complete pool lifecycle UX built across plans 01-04. This checkpoint covers all pool components, the admin direct creation flow (D-02), and the full resale-to-distribution chain (INFRA-04).

    Verification steps:
    1. Pool detail page at /pools/[id]: lifecycle stepper, chart, trade widget, position summary, how-it-works
    2. Pool creation stepper: vendor FAB and admin dashboard
    3. Admin direct pool creation (D-02): "Create Pool for Vendor" in admin Pools tab
    4. Pool management dashboard: admin Pools tab with context-dependent actions
    5. Resale-to-distribution chain (INFRA-04): set resale price -> escrow -> buyer purchase -> ship -> confirm -> distribute -> claim
    6. Pool browse page: lifecycle badges, navigation to /pools/[id]
    7. Old /pool/[id] redirect to /pools/[id]
    8. Legal language compliance: no prohibited terms
    9. Claim distribution panel on distributing/closed pools
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Pool cards on /pools show lifecycle badges
    - Clicking pool card opens /pools/[id] dedicated page
    - Lifecycle stepper shows 6 stages with purple accent on current stage
    - Trade widget has Buy/Sell tabs with SOL input
    - Admin dashboard has Pools tab with "Create Pool for Vendor" button (D-02)
    - Admin can create pool on behalf of any vendor (D-02)
    - "Set Resale Price" action creates escrow for buyer delivery flow (INFRA-04)
    - Old /pool/[id] redirects to /pools/[id]
    - No "invest", "shares", "ROI", "profit" visible on any pool page
  </acceptance_criteria>
  <done>User has visually verified the complete pool lifecycle UX including D-02 admin direct creation and INFRA-04 resale-to-distribution chain, and approved it.</done>
</task>

</tasks>

<verification>
- Build passes
- Old /pool/[id] redirects to /pools/[id]
- All pool UI components render without errors
- Legal language compliance verified
- Admin can create pools on behalf of vendors (D-02)
- Full INFRA-04 chain verified: resale price -> escrow -> purchase -> ship -> confirm -> distribute -> claim
</verification>

<success_criteria>
- Complete pool lifecycle is visually and functionally correct
- All 10 requirement IDs (POOL-01 through POOL-06, UI-03, INFRA-02, INFRA-03, INFRA-04) are addressed across plans 01-04
- D-02 admin direct pool creation verified in human checkpoint
- INFRA-04 resale-to-distribution chain verified in human checkpoint
- User approves the pool lifecycle UX in human verification checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/08-pool-lifecycle/08-04-SUMMARY.md`
</output>
