---
phase: 02.1-tokenomics-multi-treasury
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/pages/api/escrow/confirm-delivery.ts
  - src/pages/api/escrow/verify-shipment.ts
  - src/pages/api/pool/pay-vendor.ts
  - src/pages/api/pool/distribute.ts
  - src/pages/api/pool/finalize.ts
  - src/pages/api/bags/create-partner-config.ts
  - src/pages/api/bags/partner-stats.ts
  - src/pages/api/bags/claim-fees.ts
  - src/pages/api/webhooks/bags.ts
  - src/pages/api/webhooks/helius.ts
  - src/pages/api/platform/config.ts
autonomous: true
requirements: [TM-01]
must_haves:
  truths:
    - "Escrow confirm-delivery and verify-shipment use Marketplace Treasury for fee destination"
    - "Pool pay-vendor, distribute, and finalize use Pools Treasury for LuxHub fee"
    - "Bags partner-config and partner-stats use Partner Treasury"
    - "Bags claim-fees uses Pools Treasury for fee claims"
    - "Webhooks route fees to correct treasury per event type"
    - "Platform config endpoint returns all 3 treasury addresses"
    - "No file references NEXT_PUBLIC_LUXHUB_WALLET for treasury purposes (only backward-compat client display)"
  artifacts:
    - path: "src/pages/api/escrow/confirm-delivery.ts"
      provides: "Escrow fund release with Marketplace Treasury"
      contains: "getTreasury('marketplace')"
    - path: "src/pages/api/pool/distribute.ts"
      provides: "Distribution with Pools Treasury"
      contains: "getTreasury('pools')"
    - path: "src/pages/api/bags/create-partner-config.ts"
      provides: "Partner config with Partner Treasury"
      contains: "getTreasury('partner')"
  key_links:
    - from: "src/pages/api/escrow/confirm-delivery.ts"
      to: "src/lib/config/treasuryConfig.ts"
      via: "import getTreasury"
      pattern: "getTreasury\\('marketplace'\\)"
    - from: "src/pages/api/pool/distribute.ts"
      to: "src/lib/config/treasuryConfig.ts"
      via: "import getTreasury"
      pattern: "getTreasury\\('pools'\\)"
---

<objective>
Migrate all 11 endpoints from NEXT_PUBLIC_LUXHUB_WALLET to the appropriate multi-treasury wallet via getTreasury().

Purpose: Every fund-moving endpoint must route fees to the correct treasury wallet. Marketplace escrow fees go to Marketplace Treasury, pool-related fees go to Pools Treasury, and partner fees go to Partner Treasury. This enables accurate revenue tracking by source.

Output: All 11 endpoints updated, no remaining NEXT_PUBLIC_LUXHUB_WALLET references for treasury purposes.
</objective>

<execution_context>
@/home/ycstudio/LuxHub/.claude/get-shit-done/workflows/execute-plan.md
@/home/ycstudio/LuxHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-tokenomics-multi-treasury/02.1-CONTEXT.md
@.planning/phases/02.1-tokenomics-multi-treasury/02.1-RESEARCH.md
@.planning/phases/02.1-tokenomics-multi-treasury/02.1-01-SUMMARY.md

<interfaces>
From src/lib/config/treasuryConfig.ts (created in Plan 01):
```typescript
export type TreasuryType = 'marketplace' | 'pools' | 'partner';
export function getTreasury(type: TreasuryType): string;
export function getLegacyTreasury(): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate escrow and webhook endpoints to multi-treasury</name>
  <files>
    src/pages/api/escrow/confirm-delivery.ts,
    src/pages/api/escrow/verify-shipment.ts,
    src/pages/api/webhooks/bags.ts,
    src/pages/api/webhooks/helius.ts,
    src/pages/api/platform/config.ts
  </files>
  <read_first>
    - src/pages/api/escrow/confirm-delivery.ts (current NEXT_PUBLIC_LUXHUB_WALLET usage)
    - src/pages/api/escrow/verify-shipment.ts (current NEXT_PUBLIC_LUXHUB_WALLET usage)
    - src/pages/api/webhooks/bags.ts (current partner fee destination)
    - src/pages/api/webhooks/helius.ts (current treasury reference)
    - src/pages/api/platform/config.ts (current platform config display)
    - src/lib/config/treasuryConfig.ts (getTreasury API)
  </read_first>
  <action>
    For each file, apply this pattern:
    1. Add import: `import { getTreasury } from '../../../lib/config/treasuryConfig';` (adjust path depth)
    2. Find every `process.env.NEXT_PUBLIC_LUXHUB_WALLET` reference
    3. Replace with the appropriate `getTreasury()` call based on context

    **File-by-file changes:**

    **src/pages/api/escrow/confirm-delivery.ts:**
    - Find: `process.env.NEXT_PUBLIC_LUXHUB_WALLET` (used as fee destination for 3% escrow fee)
    - Replace with: `getTreasury('marketplace')` — direct sales go to Marketplace Treasury
    - Import path: `'../../../lib/config/treasuryConfig'`

    **src/pages/api/escrow/verify-shipment.ts:**
    - Find: `process.env.NEXT_PUBLIC_LUXHUB_WALLET` (used as fee destination reference)
    - Replace with: `getTreasury('marketplace')` — escrow verification is marketplace domain
    - Import path: `'../../../lib/config/treasuryConfig'`

    **src/pages/api/webhooks/bags.ts:**
    - Find: `process.env.BAGS_PARTNER_WALLET || process.env.NEXT_PUBLIC_LUXHUB_WALLET` (partner fee destination)
    - Replace with: `getTreasury('partner')` — partner fees go to Partner Treasury
    - Also check for any other NEXT_PUBLIC_LUXHUB_WALLET references and replace with appropriate treasury
    - Import path: `'../../../lib/config/treasuryConfig'`

    **src/pages/api/webhooks/helius.ts:**
    - Find: `process.env.NEXT_PUBLIC_LUXHUB_WALLET` (treasury reference in event handling)
    - Replace with: `getTreasury('marketplace')` — Helius monitors marketplace escrow events
    - Import path: `'../../../lib/config/treasuryConfig'`

    **src/pages/api/platform/config.ts:**
    - This endpoint returns platform configuration to the frontend
    - Replace single treasury with all three:
      ```typescript
      treasury: {
        marketplace: getTreasury('marketplace'),
        pools: getTreasury('pools'),
        partner: getTreasury('partner'),
      }
      ```
    - Wrap in try/catch so missing env vars don't crash the config endpoint — use 'not configured' as fallback display value
    - Import path: `'../../../lib/config/treasuryConfig'`

    IMPORTANT: Call getTreasury() inside the handler function body, NOT at module scope (SSR build safety per Phase 02 decision).
  </action>
  <verify>
    <automated>grep -r "NEXT_PUBLIC_LUXHUB_WALLET" src/pages/api/escrow/confirm-delivery.ts src/pages/api/escrow/verify-shipment.ts src/pages/api/webhooks/bags.ts src/pages/api/webhooks/helius.ts src/pages/api/platform/config.ts; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/api/escrow/confirm-delivery.ts contains `getTreasury('marketplace')`
    - src/pages/api/escrow/confirm-delivery.ts does NOT contain `NEXT_PUBLIC_LUXHUB_WALLET`
    - src/pages/api/escrow/verify-shipment.ts contains `getTreasury('marketplace')`
    - src/pages/api/escrow/verify-shipment.ts does NOT contain `NEXT_PUBLIC_LUXHUB_WALLET`
    - src/pages/api/webhooks/bags.ts contains `getTreasury('partner')`
    - src/pages/api/webhooks/bags.ts does NOT contain `NEXT_PUBLIC_LUXHUB_WALLET`
    - src/pages/api/webhooks/helius.ts contains `getTreasury('marketplace')`
    - src/pages/api/platform/config.ts contains `getTreasury('marketplace')` and `getTreasury('pools')` and `getTreasury('partner')`
    - All 5 files import from `treasuryConfig`
  </acceptance_criteria>
  <done>All 5 escrow/webhook/platform endpoints use correct treasury via getTreasury(). No NEXT_PUBLIC_LUXHUB_WALLET references remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate pool and bags endpoints to multi-treasury</name>
  <files>
    src/pages/api/pool/pay-vendor.ts,
    src/pages/api/pool/distribute.ts,
    src/pages/api/pool/finalize.ts,
    src/pages/api/bags/create-partner-config.ts,
    src/pages/api/bags/partner-stats.ts,
    src/pages/api/bags/claim-fees.ts
  </files>
  <read_first>
    - src/pages/api/pool/pay-vendor.ts (current NEXT_PUBLIC_LUXHUB_WALLET for 3% fee)
    - src/pages/api/pool/distribute.ts (current NEXT_PUBLIC_LUXHUB_WALLET for royalty destination)
    - src/pages/api/pool/finalize.ts (current treasury reference)
    - src/pages/api/bags/create-partner-config.ts (current partner wallet fallback)
    - src/pages/api/bags/partner-stats.ts (current partner wallet)
    - src/pages/api/bags/claim-fees.ts (current fee claim wallet)
    - src/lib/config/treasuryConfig.ts (getTreasury API)
  </read_first>
  <action>
    For each file, apply same import + replacement pattern:

    **src/pages/api/pool/pay-vendor.ts:**
    - Find: `process.env.NEXT_PUBLIC_LUXHUB_WALLET` (LuxHub 3% fee on graduation)
    - Replace with: `getTreasury('pools')` — pool graduation fees go to Pools Treasury
    - Import: `import { getTreasury } from '../../../lib/config/treasuryConfig';`

    **src/pages/api/pool/distribute.ts:**
    - Find: `const treasuryWallet = process.env.NEXT_PUBLIC_LUXHUB_WALLET;` (line ~125)
    - Replace with: `const treasuryWallet = getTreasury('pools');`
    - Remove the null check that follows (`if (!treasuryWallet)`) since getTreasury throws on missing
    - Update the recipient label from 'LuxHub 3% royalty' to 'Pools Treasury 3% royalty'
    - Import: `import { getTreasury } from '../../../lib/config/treasuryConfig';`

    **src/pages/api/pool/finalize.ts:**
    - Find: `process.env.NEXT_PUBLIC_LUXHUB_WALLET` (treasury reference)
    - Replace with: `getTreasury('pools')` — finalization is pool domain
    - Import: `import { getTreasury } from '../../../lib/config/treasuryConfig';`

    **src/pages/api/bags/create-partner-config.ts:**
    - Find: `process.env.BAGS_PARTNER_WALLET || process.env.NEXT_PUBLIC_LUXHUB_WALLET` (partner wallet fallback)
    - Replace with: `getTreasury('partner')` — partner config uses Partner Treasury
    - Import: `import { getTreasury } from '../../../lib/config/treasuryConfig';`

    **src/pages/api/bags/partner-stats.ts:**
    - Find: `process.env.BAGS_PARTNER_WALLET || process.env.NEXT_PUBLIC_LUXHUB_WALLET`
    - Replace with: `getTreasury('partner')`
    - Import: `import { getTreasury } from '../../../lib/config/treasuryConfig';`

    **src/pages/api/bags/claim-fees.ts:**
    - Find: `process.env.NEXT_PUBLIC_LUXHUB_WALLET` or similar treasury reference
    - Replace with: `getTreasury('pools')` — fee claims are from pool trading fees
    - Import: `import { getTreasury } from '../../../lib/config/treasuryConfig';`

    IMPORTANT: Call getTreasury() inside handler function body, not at module scope.
  </action>
  <verify>
    <automated>grep -r "NEXT_PUBLIC_LUXHUB_WALLET" src/pages/api/pool/pay-vendor.ts src/pages/api/pool/distribute.ts src/pages/api/pool/finalize.ts src/pages/api/bags/create-partner-config.ts src/pages/api/bags/partner-stats.ts src/pages/api/bags/claim-fees.ts; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/api/pool/pay-vendor.ts contains `getTreasury('pools')`
    - src/pages/api/pool/distribute.ts contains `getTreasury('pools')`
    - src/pages/api/pool/distribute.ts does NOT contain `NEXT_PUBLIC_LUXHUB_WALLET`
    - src/pages/api/pool/finalize.ts contains `getTreasury('pools')`
    - src/pages/api/bags/create-partner-config.ts contains `getTreasury('partner')`
    - src/pages/api/bags/partner-stats.ts contains `getTreasury('partner')`
    - src/pages/api/bags/claim-fees.ts contains `getTreasury('pools')`
    - All 6 files import from `treasuryConfig`
    - None of the 6 files contain `NEXT_PUBLIC_LUXHUB_WALLET`
  </acceptance_criteria>
  <done>All 6 pool/bags endpoints use correct treasury via getTreasury(). Pool endpoints use Pools Treasury, partner endpoints use Partner Treasury. No NEXT_PUBLIC_LUXHUB_WALLET references remain.</done>
</task>

</tasks>

<verification>
```bash
# No NEXT_PUBLIC_LUXHUB_WALLET remaining in any migrated endpoint (11 files)
grep -rn "NEXT_PUBLIC_LUXHUB_WALLET" \
  src/pages/api/escrow/confirm-delivery.ts \
  src/pages/api/escrow/verify-shipment.ts \
  src/pages/api/pool/pay-vendor.ts \
  src/pages/api/pool/distribute.ts \
  src/pages/api/pool/finalize.ts \
  src/pages/api/bags/configure-fee-share.ts \
  src/pages/api/bags/create-partner-config.ts \
  src/pages/api/bags/partner-stats.ts \
  src/pages/api/bags/claim-fees.ts \
  src/pages/api/webhooks/bags.ts \
  src/pages/api/webhooks/helius.ts \
  src/pages/api/platform/config.ts
# Expected: 0 matches

# All files import getTreasury
grep -c "getTreasury" src/pages/api/escrow/confirm-delivery.ts src/pages/api/pool/distribute.ts src/pages/api/bags/create-partner-config.ts
# Expected: each file has at least 1 match

# TypeScript compiles without errors
npx tsc --noEmit --skipLibCheck 2>&1 | head -20
```
</verification>

<success_criteria>
- All 11 endpoints migrated from NEXT_PUBLIC_LUXHUB_WALLET to getTreasury()
- Escrow endpoints use 'marketplace' treasury
- Pool endpoints use 'pools' treasury
- Bags partner endpoints use 'partner' treasury
- Platform config returns all 3 treasury addresses
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-tokenomics-multi-treasury/02.1-02-SUMMARY.md`
</output>
