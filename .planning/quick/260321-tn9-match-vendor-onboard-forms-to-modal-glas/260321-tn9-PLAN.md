# Quick Task 260321-tn9: Match vendor onboard forms to modal glass border pattern

## Task
Align VendorOnboard.module.css form inputs, buttons, and card to match the chrome glass gradient border pattern used in BuyModal and MakeOfferModal.

## Changes
1. Inputs → transparent bg + inset box-shadow (not solid bg + 1px border)
2. Buttons → glass border mask + radial glow (not solid gradient fill)
3. Wizard card → glass surface + gradient mask border
4. CSS vars → aligned to modal values
